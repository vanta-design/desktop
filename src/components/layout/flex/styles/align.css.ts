import { style } from '@/utils/style';

export default {
  start: style({
    alignItems: 'flex-start',
  }),
  center: style({
    alignItems: 'center',
  }),
  end: style({
    alignItems: 'flex-end',
  }),
  stretch: style({
    alignItems: 'stretch',
  }),
};
