import { spacing } from '@/tokens/attribute.css';
import { style } from '@/utils/style';

export const boundingBox = style({
  display: 'inline-flex',
  alignItems: 'center',
  padding: spacing[4],
  placeItems: 'center',
  cursor: 'pointer',
});
